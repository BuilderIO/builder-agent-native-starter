# Custom health check message
PING_MESSAGE=pong

# Neon Postgres — injected automatically when the project has a managed database.
# For local development, copy values from the Builder dashboard.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
